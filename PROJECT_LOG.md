# 通用图片格式转换工具 - 工程记录与开发日志

## 📌 项目简介
一个基于 Python + Tkinter 开发的 Windows 桌面级图片批量格式转换工具。从最初的“WebP转JPG”小脚本，经过多轮迭代，现已升级为支持多格式输入输出、多核加速、任务队列、预览对比、日志导出和容错处理的成熟工具。

## 🏗️ 技术栈与核心架构
- **语言**: Python 3.12+
- **核心依赖**: `Pillow` (图片编解码), `tkinterdnd2` (拖拽支持), `multiprocessing` (多核并发), `concurrent.futures` (进程池管理)
- **打包工具**: `PyInstaller` (onedir 模式，UPX 压缩)
- **CI/CD**: GitHub Actions (自动构建 Windows 可执行文件)
- **核心文件**:
  - `webp2jpg_gui.py`: 主程序源码 (GUI + 业务逻辑)
  - `.github/workflows/build.yml`: GitHub Actions 自动打包配置
  - `README.md`: 项目说明文档
  - `PROJECT_LOG.md`: 本工程记录

## ✅ 已实现功能清单 (截至当前版本)

### 1. 格式支持
- **输入**: `.webp`, `.png`, `.jpg`, `.jpeg`, `.bmp`, `.tiff`, `.gif`
- **输出**: `JPG`, `PNG`, `WEBP`, `BMP`
- **智能联动**: 输出为无损格式（PNG/BMP）时，质量下拉框自动禁用。

### 2. 性能与稳定性
- **多核并发**: 支持手动调节并发核心数，默认使用 CPU 最大核心数。
- **分批处理**: 每 500 张图片一批，防止大文件导致内存溢出。
- **容错机制**: 检测文件损坏、被占用、无权限，跳过并记录；转换中途失败自动清理残缺文件。
- **智能覆盖**: 支持“覆盖同名文件”、“跳过同名文件”、“重命名保存 (如 _1)”。

### 3. 用户体验 (UI/UX)
- **多任务队列**: 支持拖拽多个文件夹或单个文件加入队列，支持移除、清空。
- **转换前预览**: 双击队列文件，在内存中模拟转换，对比原图与转换后效果及体积。
- **转换后对比**: 双击左侧成功日志，对比实际转换前/后的尺寸、大小和压缩率。
- **独立错误日志**: 右侧独立显示错误日志（含完整路径），双击可直接在资源管理器中定位。
- **日志导出**: 一键将运行和错误日志导出为 `.txt` 文件。
- **配置持久化**: 自动记住用户的上次设置（输出格式、质量、覆盖策略等）。
- **实时进度条**: 转换过程中实时显示总进度。
- **取消功能**: 支持中途取消批量转换任务。
- **画质通俗化**: 质量选项从数字改为“极高画质 (原图100%质量)”等易懂的文字描述。

### 4. 工程与构建优化
- **全面使用 `pathlib`**: 代码结构更现代、规范。
- **启动提速**: 打包模式改为 `--onedir`，大幅提升 exe 启动速度。
- **体积压缩**: 引入 UPX 压缩，排除无用模块（如 PyQt5），减小打包体积。
- **自动构建**: 推送代码到 GitHub 自动触发构建，生成 `ImgConverter.zip`。

## 🚀 历史迭代节点摘要
1. **V1**: 纯命令行 WebP 转 JPG 脚本 (Pillow)。
2. **V2**: 增加命令行参数 (`-q`, `-d`, `--no-recursive`)。
3. **V3**: 打包为单文件 exe (PyInstaller, GitHub Actions)。
4. **V4**: 引入 Tkinter GUI，支持界面操作。
5. **V5**: 引入多核并发 (`ProcessPoolExecutor`)，添加线程防卡顿。
6. **V6**: 添加任务队列、进度条、取消按钮、拖拽支持、日志导出、错误日志独立路径显示、分批处理。
7. **V7**: 支持拖拽文件、引入文件预览、全面重构为 `pathlib`、增加画质百分比文字描述。
8. **V8**: 加入内存模拟转换预览 (转换前效果) 和实际对比预览 (转换后效果)。
9. **V9 (当前)**: 升级为通用格式转换器，支持 WebP/PNG/JPG/BMP/TIFF/GIF 输入，输出 JPG/PNG/WEBP/BMP，完善 README 和工程记录。

## 💡 下一步待办与优化方向 (未完成)
- [ ] **图片尺寸缩放**: 增加“最长边限制 1920px”等选项，降低分辨率以大幅节省体积。
- [ ] **自定义输出目录**: 支持将转换后的图片输出到指定文件夹，并保持原目录结构。
- [ ] **命令行模式 (CLI)**: 支持不带 GUI 运行，方便加入 Windows 任务计划程序。
- [ ] **深色模式**: 引入 `ttkbootstrap` 或 `ttkthemes`，提升夜间使用体验。
- [ ] **系统通知与托盘**: 转换完成时弹出 Windows 气泡通知。
- [ ] **右键菜单集成**: 通过注册表集成，在任意文件夹右键选择“用 ImgConverter 转换”。
- [ ] **自定义图标**: 打包时增加 `--icon=app.ico`，替换默认的齿轮图标。
- [ ] **去除部分依赖**: 如果不需要拖拽，可以考虑去除 `tkinterdnd2`，进一步减小体积。

---
*最后更新: 2026-05-24*